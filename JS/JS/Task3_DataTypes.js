// Напишите функцию, которая принимает строку и выводит в консоль последний символ.

function Task3(str) {
  const lastChar = str[str.length - 1]
  console.log(lastChar)
}

Task3('Привет') // т
Task3('Hello') // o
